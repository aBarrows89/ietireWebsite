<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100">
  <!-- Outer tire ring -->
  <circle cx="50" cy="50" r="48" fill="#1a1a1a" stroke="#333" stroke-width="2"/>
  <!-- Tread pattern -->
  <circle cx="50" cy="50" r="42" fill="none" stroke="#2a2a2a" stroke-width="8" stroke-dasharray="10 5"/>
  <!-- Sidewall -->
  <circle cx="50" cy="50" r="32" fill="#252525" stroke="#333" stroke-width="1"/>
  <!-- Rim outer - red accent for IE Tire brand -->
  <circle cx="50" cy="50" r="24" fill="#dc2626"/>
  <!-- Rim inner -->
  <circle cx="50" cy="50" r="18" fill="#1a1a1a"/>
  <!-- Center hub -->
  <circle cx="50" cy="50" r="8" fill="#444"/>
  <!-- IE text -->
  <text x="50" y="55" font-family="Arial, sans-serif" font-size="12" font-weight="bold" fill="white" text-anchor="middle">IE</text>
</svg>
